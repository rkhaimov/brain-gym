# Соотношение

* Тек. работа (в среднем 60% времени)
* Теория/Практика (в идеале 30% времени)
* Алгоритмы (остальное, по хорошему касаться одной задачи в день)

# Теория

Изучить ./learning.md
Изучить архитектуры популярных решений (antd, shadcn, material, radix ui)

# Практика

Виртуализация списков на React (sync и async)
WebWorker heavy work + indexDB LRU cache
Попробовать трюк с dry-run (+скачиваем nexus serve repository, tgz + offline install автоматическая верификация версий)
Zustand API
Tailwind на уровне дизайн-системы (конфиг, пресеты, @apply разумно).
Next.js (App Router) performance: route-level code splitting, RSC streaming, ISR, dynamic/force-static, caching headers, image/font оптимизация.
Service Worker
React 19 (RSC, concurrent features). Отладить runtime
TanStack Query (SSR)
PWA/Service Worker: Workbox/кастомные SW, оффлайн-fallback, стратегии кэширования.
интеграция Sentry (frontend+server), метрики в Grafana (Prom → Loki/Tempo знание приветствуется), алёртинги.
Redux повторить (toolkit)
NestJS, PostgressDB, MySQL
Node.js, Express, Sequelize, PostgreSQL, ClickHouse

