import { narrow } from './index';
import { number } from './primitives';
import { list, struct } from './hotn';

test('Plug', () => {
  expect(true).toBeTruthy();
});

describe('Constraints composition', () => {});

describe('Constraints composition with possible capabilities narrowing', () => {});

describe('Constraints composition with possible capabilities widening', () => {});

describe('Type augmentations', () => {});
