* Flutter isolates крос платформа android/ios + тонкий нативный слой bluetooth модуля сопряжения/подключения к OBD и
  старта foreground сервиса 
* Модуль подключения к OBD с health check, разные частоты на параметры 
* Батчинг параметров + delta encoding для оптимизации трафика 
* MQTT с QoS 1 но важна идемпотентность на BE т. к. могут быть дубли 
* Локальная очередь SQLite. Backpressure + Drop policy + Offline mode
* Адаптивная частота от батареии и скорости движения 
* Вся модель хорошо описывается реактивной моделью, в целом из неё хорошо выражается стейт машина
* Отдельная библиотека на dart + компиляция на js